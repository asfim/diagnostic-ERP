@php($editing = isset($faq) && $faq)
<form action="{{ $editing ? route('faqs.update', $faq) : route('faqs.store') }}" method="POST">@csrf @if($editing) @method('PUT') @endif
@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<div class="mb-3"><label class="form-label">Question *</label><input name="question" class="form-control" value="{{ old('question', $faq->question ?? '') }}" required></div><div class="mb-3"><label class="form-label">Answer *</label><textarea name="answer" class="form-control" rows="7" required>{{ old('answer', $faq->answer ?? '') }}</textarea></div><div class="row g-3"><div class="col-md-6"><label class="form-label">Sort Order</label><input type="number" name="sort_order" class="form-control" value="{{ old('sort_order', $faq->sort_order ?? 0) }}" min="0" required></div><div class="col-md-6 d-flex align-items-end"><div class="form-check form-switch mb-2"><input type="checkbox" name="status" value="1" class="form-check-input" id="faqStatus" @checked(old('status', $faq->status ?? true))><label class="form-check-label" for="faqStatus">Show on public FAQ page</label></div></div></div><div class="text-end mt-4"><a href="{{ route('faqs.index') }}" class="btn btn-secondary">Cancel</a> <button class="btn btn-primary"><i class="fa-solid fa-save me-1"></i>{{ $editing ? 'Update FAQ' : 'Save FAQ' }}</button></div></form>
